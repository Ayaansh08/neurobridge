import React from 'react';

// Deprecated in Round 4: Sensory Comfort controls moved fully into SettingsView.
export const ComfortPanel: React.FC<{ isOpen?: boolean; onClose?: () => void }> = () => null;
